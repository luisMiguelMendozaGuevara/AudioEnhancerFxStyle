# Versiones EXACTAS probadas (lockfile de restricciones).
#
# Uso recomendado para builds reproducibles:
#   pip install -r requirements-dev.txt -c constraints.txt
#   pip install . -c constraints.txt
#
# `requirements*.txt` declara los rangos compatibles; este archivo fija las
# versiones que pasaron la suite (155 tests) y el empaquetado con PyInstaller
# en Windows x64 / Python 3.11. Actualizar tras validar una subida.
numpy==2.4.6
scipy==1.17.1
PySide6==6.11.2
shiboken6==6.11.2
PyAudioWPatch==0.2.12.8
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
packaging==26.3
ruff==0.16.8
pyinstaller==6.22.3
altgraph==0.17.5
pefile==2024.8.26
pywin32-ctypes==0.2.3
